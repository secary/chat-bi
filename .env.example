# Business/app/admin database. Docker demos expose this MySQL on host port 3307.
CHATBI_DB_HOST=127.0.0.1
CHATBI_DB_PORT=3307
CHATBI_DB_NAME=chatbi_demo
CHATBI_DB_USER=demo_user
CHATBI_DB_PASSWORD=demo_pass

# Trace logs default into CHATBI_DB_NAME as table chatbi_logs_trace_log.
# Override CHATBI_LOG_DB_NAME only if you need a separate log schema.

# Max LLM rounds per user message (each round may execute one Skill).
CHATBI_AGENT_MAX_STEPS=8

# JWT for authenticated API. Use a strong random secret outside local demo.
CHATBI_JWT_SECRET=change-me-in-production
CHATBI_JWT_EXP_HOURS=168

# Seed admin on backend startup. Existing users keep their password by default.
CHATBI_DEFAULT_ADMIN_USERNAME=admin
CHATBI_DEFAULT_ADMIN_PASSWORD=admin123
# To force-reset the seed admin password, set this to 1 temporarily and restart backend.
# CHATBI_DEFAULT_ADMIN_RESET_PASSWORD=0
