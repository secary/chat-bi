name: ChatBI CI

"on":
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: chatbi-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-tests:
    name: Backend pytest
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip

      - name: Install system dependencies
        run: sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip uv
          uv sync --frozen --no-install-project

      - name: Lint
        run: |
          .venv/bin/ruff check backend/ scripts/ tests/
          .venv/bin/black --check backend/ scripts/ tests/

      - name: Run Python tests
        env:
          PYTHONPATH: .
          CHATBI_AUTH_ENABLED: "false"
          CHATBI_MEMORY_DISABLED: "1"
          CHATBI_VISION_DISABLED: "1"
          CHATBI_PDF_SUMMARY_DISABLED: "1"
        run: .venv/bin/python scripts/run_tests.py all -- -q

  frontend-tests:
    name: Frontend lint, test, build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test

      - name: Build
        run: npm run build
