# TODO

## 近期优先

- [ ] 梳理单 agent 与多 agent 的智能路由决策路径。
- [ ] 复盘 workflow 多任务链路：确认 route_sequence 切线、子任务依赖、specialist ReAct 执行、汇总审计与 fallback 的端到端行为。
- [ ] 补齐多 agent 路由场景覆盖。
- [ ] 优化记忆管理与会话管理：梳理摘要刷新、长期偏好合并、会话恢复、pending assistant 与多轮上下文边界。
- [ ] 优化后端 Skill 脚本与执行链路。
- [ ] 提升测试框架自动化程度。
- [ ] 将测试框架与 harness 能力进一步集成。
- [ ] 分析零眸测试库并沉淀接入方案。
- [x] 优化或彻底删除 PDF 导出相关残留。
- [ ] 重新设计核心界面体验。
- [ ] 提供更傻瓜式的一键启动方式。

## 非必需

- [ ] 支持通过自然语言触发数据源接入与切换，优先落地同构 MySQL 多库场景。
- [ ] 为“自然语言接库”补一个独立 Skill，例如 `chatbi-datasource-onboarding`。
- [ ] 在接库流程中加入确定性校验：连接测试、schema 摘要、默认库切换、失败原因回传。
- [ ] 明确多数据库范围：仅支持同构业务库，还是支持异构 schema + 独立语义层。
- [ ] 设计聊天内的数据源切换体验，避免暴露原始 `db_connection_id`。
- [ ] 定义“接入成功但暂不可问数”的状态，用于异构库待语义映射场景。
- [ ] 将“数据库连接”概念升级为“数据源”模型，预留 `slug`、`display_name`、`enabled`、`semantic_profile` 等字段。
- [ ] 让 Skill 在执行时同时感知数据库连接和语义 profile，而不只是 `CHATBI_DB_*`。
- [ ] 为新数据源补充 schema 识别与语义映射草稿生成流程。
