| 250  | 生产 CD 绑定实际 GitHub Environment 名称 `chatbi-prod`，确保部署 job 能读取该环境下的 secrets，并同步更新 CI/CD 文档 | `git diff --check`；`ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts "ok #{path}" }' .github/workflows/deploy-prod.yml` |
| 249  | 生产 CD 接入 Tailscale 方案 A：部署 job 在 SSH 前用 `tailscale/github-action@v4` 临时加入 tailnet，并通过 `TAILSCALE_AUTHKEY` 与 `PROD_SSH_HOST` ping 校验访问生产机 | `git diff --check`；`ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts "ok #{path}" }' .github/workflows/deploy-prod.yml` |
| 248  | 生产 CD 增加部署前 preflight：在连接服务器前验证 `launch.sh` 语法、prod compose 配置，并用标准库 unittest 跑 launch 脚本专项测试；文档同步说明手动发布也会先验证 | `bash -n scripts/launch.sh`；`docker compose -f docker-compose.prod.yml config`；`PYTHONPATH=. .venv/bin/python -m unittest tests.test_launch_script`；`git diff --check`；`ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts "ok #{path}" }' .github/workflows/deploy-prod.yml` |
| 247  | 新增生产 CD workflow：基于 `scripts/launch.sh` 通过 GitHub Actions SSH/rsync 同步代码到服务器，保留或注入生产 `.env`，并在远端执行 `launch.sh --no-open` 完成 compose 构建与健康检查；同步更新 CI/CD 文档 | 文档/workflow 改动，未跑测试；`git diff --check` |
| 246  | 数据源接入页移除左侧“暂无已保存数据源”空状态占位；无保存数据源时“新建数据源”按钮直接贴近当前生效连接卡片 | `.venv/bin/python scripts/format_code.py frontend/src/pages/DataSourcesPage.tsx`；`npm run build`；`git diff --check` |
| 245  | 重做用户管理页：新增账号统计摘要、紧凑创建表单、状态/角色更清晰的用户列表组件，支持行内改角色、重置密码和启停账号，并避免停用当前登录用户 | `.venv/bin/python scripts/format_code.py frontend/src/pages/UserAdminPage.tsx frontend/src/components/UserAdminTable.tsx`；`npm run build`；`git diff --check` |
| 244  | LLM 配置表单拆清“模型名”和“备注名”：模型名作为常驻输入项，选择厂商自动填推荐模型名，备注名留空时按模型名保存 | `.venv/bin/python scripts/format_code.py frontend/src/pages/LlmConfigPage.tsx`；`npm run build`；`git diff --check` |
| 243  | LLM 编辑已保存模型时 API Key 输入框改为禁用密文展示，保存编辑沿用原 profile 的 API Key 做未落库 probe 和更新，不再要求重新输入 Key | `.venv/bin/python scripts/format_code.py backend/routes/admin_llm_profiles_route.py frontend/src/api/client.ts frontend/src/pages/LlmConfigPage.tsx tests/test_admin_llm_profiles_probe.py`；`PYTHONPATH=. .venv/bin/python scripts/run_tests.py admin -- -q tests/test_admin_llm_profiles_probe.py`；`npm run build`；`git diff --check` |
| 242  | LLM 已保存模型去掉重复的“可用”状态标签；新增编辑按钮，可将已保存模型载入下方表单，重新填写 API Key 后仍按“先测试通过、再保存/启用”流程更新配置，并支持取消编辑 | `.venv/bin/python scripts/format_code.py frontend/src/pages/LlmConfigPage.tsx`；`npm run build`；`git diff --check` |
| 241  | 升级 `PyJWT` 2.12.1 → 2.13.0，并同步 `uv.lock`，修复 `pip-audit` 报出的 4 个 PyJWT 已知漏洞 | `uv lock`；`PYTHONPATH=. .venv/bin/python scripts/run_tests.py auth-memory -- -q tests/test_auth_tokens.py tests/test_auth_deps_disabled.py tests/test_default_admin_seed.py`；`uv export --frozen --format requirements.txt --no-dev --no-emit-project --no-emit-workspace --output-file python-audit-requirements.txt`；本地 `pip-audit` 联网验证因沙箱网络授权被拒未完成，待 CI 复跑确认 |
| 240  | 生产式启动环境文件命名统一为 `.env`：prod compose 只读取 `.env`，`launch.sh` 缺少 `.env` 时才从 `.env.example` 生成，测试同步覆盖 `.env` 保留行为 | `.venv/bin/python scripts/format_code.py scripts tests/test_launch_script.py`；`PYTHONPATH=. .venv/bin/python scripts/run_tests.py foundation -- -q tests/test_launch_script.py`；`docker compose -f docker-compose.prod.yml config` |
| 239  | 强化生产启动用户种子写入：prod compose 等 `chatbi_demo.app_user` 可查后再启动 app，后端 seed 重试从 5 次扩展到 30 次，并让失败日志带出具体用户名 | `docker compose -f docker-compose.prod.yml config`；`.venv/bin/python scripts/format_code.py backend/main.py backend/default_admin_seed.py`；`PYTHONPATH=. .venv/bin/python scripts/run_tests.py auth-memory -- -q tests/test_default_admin_seed.py` |
